{
  "name": "maintainer-shield",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered GitHub Action that detects slop PRs, triages issues, and scores contributor reputation for OSS maintainers",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  }
}
